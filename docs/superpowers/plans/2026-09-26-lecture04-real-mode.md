# Лекция №4: 16-битный журнал в QEMU

Решение пользователя — исполнять NASM, объяснять алгоритмы на C и затем
скоммитить/отправить весь рабочий результат.

- [x] Реализовать `boot16.asm` (сектор 512 байтов, BIOS INT 13h, чтение 17
      секторов в 1000:0000, проверка ошибки) и шесть версий `journal16.asm`.
- [x] Использовать BIOS INT 10h для версии 1, VGA напрямую для версии 2,
      BIOS INT 16h для версии 3, IRQ1/IVT/очередь для версий 4–6,
      целочисленные средние для версии 5, BIOS INT 13h Extensions для
      записи/загрузки диска и проверок формата `GR16` в версии 6.
- [x] Переписать Docker build/run/check и проверить шестистадийный сценарий
      QEMU, в том числе сохранение/перезапуск/испорченный диск и ввод noVNC.
- [x] Переписать действующие HTML/PDF и LaTeX на 16-битный маршрут;
      подписать C только как пояснение; архивировать прежние x86-64 материалы.
- [x] Обновить README, AGENTS, презентационный реестр, активные материалы;
      визуально проверить PDF/HTML, собрать через `make lecture4-journal`.
- [ ] Проверить весь git diff, убедиться в отсутствии секретов и generated
      файлов, выполнить явный запрос пользователя на commit + push.

Действующий исходник описан в
`../specs/2026-09-26-lecture04-real-mode-design.md`.
