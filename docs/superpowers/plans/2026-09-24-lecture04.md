# План реализации лекции №4

Основание: согласованная спецификация `../specs/2026-09-24-lecture04-design.md`.
Реализация в текущей сессии; сохранять предыдущие изменения реорганизации.

1. Написать `tex/lectures/lecture04_intro.tex` (компоненты, CPU, соединения)
   и `lecture04_memory_execution.tex` (память, кэш, I/O, C/NASM, итог).
   Подключить вместо каркаса лекции 4 в главе 2. Сохранить семинар.
2. Добавить исполняемые демонстрации в `demos/lecture04/`: C-проверка размеров,
   байтов и вызов NASM-функции; NASM сохраняет RBX согласно System V ABI.
   Отдельный Dockerfile с GCC/NASM; проверка реального результата 10,25,30.
3. Создать `slides/lecture04/index.html` на общей теме. План: 55 основных,
   8 дополнительных слайдов. К каждому добавить заметки и интервалы времени.
   Общий footer должен позволять лекции задать название дополнений.
4. Добавить запись в реестр, цели `lecture4-check`, `lecture4-slides`,
   `lecture4`, описание README и сборку/публикацию лекции 4 в CI.
5. Проверить расчёты скриптом `scripts/check-lecture04.py`, запустить
   `make lecture4`, проверить предупреждения и отчёт вёрстки, визуально
   просмотреть схемы в Firefox и PDF. После правок общего footer проверить
   прежний вариант лекции 2. Проверить `git diff --check`.

Без коммита и отправки в remote: пользователь запросил подготовку материалов.

## Результат проверки

- Реализованы два фрагмента пособия, 55 основных и 8 дополнительных слайдов,
  три SVG-схемы и Docker-демонстрация C/NASM.
- `make lecture4`: пособие собрано; все 17 расчётных проверок и проверка
  настоящих инструкций/результата NASM прошли.
- После правок переносов `make pdf` не сообщает overfull в файлах лекции 4.
  Сохранено небольшое underfull в таблице, просмотрено визуально.
- `make lecture4-slides lecture2-slides lecture2-original`: все три комплекта
  прошли проверки; у lecture04 63 страницы PDF 16:9, issues/errors пусты.
- Firefox: 63 слайда, 63 блока заметок, все изображения загружены,
  выходов за безопасную область не обнаружено. Проверены титул и схемы.
- Визуально проверены страницы PDF со схемами прерывания и двух потоков,
  начало лекции и машинная трассировка в PDF пособия.
- CI сконфигурирован для лекции 4; отправка в remote и публикация
  в текущей сессии не выполнялись.
